The ps command is used to list the processes running on the computer.
On its own the command lists the processes started by the user.
With the parameter x the command lists all of the processes running.
